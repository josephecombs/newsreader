<h1><%= feed.escape("title") %></h1>
<h3><%= feed.escape("url") %></h3>

<ul>
  <% feed.entries().each( function (entry) {%>
    <li><%= entry.escape("title") %></li>
  <% }) %>
</ul>

<button class="refresh">Refresh</button>